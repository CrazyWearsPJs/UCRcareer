/**
 * Export settings for application
 */

module.exports = {
	dbSettings: {
	    host:     "10.0.2.15"
	  , port:     "8081"
	  , database: "UCRcareers"
	}
	//uri: "mongodb://" + dbSettings.host + ":" + dbSettings.port + "/" + dbSettings.database;
};

